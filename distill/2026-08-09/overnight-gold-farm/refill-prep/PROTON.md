# Refill-prep — Proton (map-only)

**Status:** STUB · not ACTIVE hydra lane · **keep-8 rank 5** (`EV-QUEUE.md`)  
**SSoT:** `~/.xbgst/bounty-distill/2026-08-07/ROI.md` · `scopes/proton.md` · `playbooks/proton.md`  
**Policy:** MAP ≠ COMPLETE · no exploit steps · own assets only when open

## Why keep-8

- Direct / FP path with **high published ceiling** (mail/web classes; max band cited in ROI/HUNT-NOW).  
- Skill fit for web/authz + dual own-account IDOR class once research mailboxes exist.  
- Email-submit workflow is **documentable** offline; does not require BC Get-Credentials thrash.  
- Strong **refill** candidate after first H1–H3 COMPLETE frees a lane — not overnight OVERFIT (Aiven holds OVERFIT).

## First human action

1. Confirm program policy + in-scope surfaces from public Proton security / bug-bounty page (browser; no thrash).  
2. Create **research-only** Proton accounts for dual-user tests (or confirm existing research mailboxes).  
3. Store access via **the-janitor / op titles only** — never paste passwords into distill or hydra STATUS.  
4. Note submit channel (email/form) and any required PGP/header rules into a future `SCOPE-NOTES.md` when lane spawns.

## First agent action when open

1. Scaffold lane dir only after `hydra-refill` (or orch assign): `TASK.md` · `SCOPE-NOTES.md` from `scopes/proton.md` + FULL/PARTIAL fidelity tags.  
2. Write `FIRST-5-TESTS.md` as **own-mailbox / own-Drive** checklists (authz, share links, cross-account) — no third-party target probing.  
3. Run passive policy extract into `REPORT.md` map section; produce `evidence/OWN-ASSET.md` with account handles/IDs only.  
4. COMPLETE only via `tracker/COMPLETE-GATE.md` (E1–E8 + GATE-PASS).

## Kill criteria

| Kill if | Action |
|---------|--------|
| Scope extract shows out-of-scope for planned class | Stay map; do not force web class |
| Only one research account available | PARTIAL dual-auth path; no fake dual evidence |
| Program paused / policy conflict with local rules | Defer; pick next EV-QUEUE refill |
| Pressure to COMPLETE without own-asset results | **Refuse**; NEGATIVE-CLOSE or MAPPED |
| CAPTCHA / account lock storms | Stop; human recovery; no bypass tooling |

## Non-goals

Live exploit payloads · catalog thrash · using personal primary mailbox as “bounty lab” without isolation · marking COMPLETE to exercise refill.
