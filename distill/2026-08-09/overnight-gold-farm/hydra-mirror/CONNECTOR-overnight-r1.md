# CONNECTOR overnight-r1 — gold farm cross-axis

**UTC write:** 2026-08-09 (session overnight-gold-farm)  
**Role:** connector (gx-connector-r1)  
**Axes:** `gold_ev_per_hour↑` `time_to_first_submit↑` `substrate_fertile↑` `human_gate_throughput↑` `evidence_fidelity↑` `safety_in_policy↑` `ship_hygiene↑` `idle_utilization↑`  
**Scope:** `~/.xbgst/hydra-bounty` · `~/.xbgst/bounty-distill` · `~/Projects/open-bug-bounties`  
**Plan SSoT:** `~/.xbgst/plans/2026-08-08-overnight-gold-farm.md`  
**Rule restated:** `MAP ≠ COMPLETE`. COMPLETE only with own-asset evidence paths. No CAPTCHA / Get-Credentials thrash. Dual-auth race DONE; human freezes remain.

---

# State

- **inf:** Three orch shapes (stack CLI, wrap workflow, grok CLI) still share **one economy**: map+L3 packs are free; cash needs human doors. Overnight gold is not “more recon” — it is **rank once → overfit → idle prep without thrash** [strong] — axes: gold_ev_per_hour, idle_utilization, safety_in_policy  
- **inf:** Keep-8 SSoT (`ROI.md`) and live hydra H1–H3 are **aligned but gate-locked**: Aiven / Auth0 / Google all MAPPED, **zero COMPLETE**, refill never fired [strong] — axes: evidence_fidelity, ship_hygiene  
- **inf:** Whip is **alive again** (loop pid present; 2026-08-09T01:01Z tick: `hydra_ok sekhmet_abc_alive`, `op_aivenA=no op_aivenB=no`, `no_COMPLETE no_spawn no_refill`) after a dead gap (`hydra_no sekhmet_dead` at 00:40Z). Substrate recovered without inventing COMPLETE [strong] — axes: substrate_fertile, idle_utilization  
- **inf:** Missing `tracker/EV-QUEUE.md` is the **only agent-owned bottleneck** left before honest overfit lock; plan M02 not landed [strong] — axes: gold_ev_per_hour, time_to_first_submit  
- **inf:** Dual-auth `AUTONOMOUS-READY` freezes #2/#3/#6 **are** hydra H1/H2/H3 — same three keys, two names. Re-running dual-auth race or re-mapping doors is thrash [strong] — axes: evidence_fidelity, idle_utilization  
- **risk:** Scoring by marketing ceiling (Shopify $200k, Google RCE tier) without **gate_factor** → alphabet/ceiling thrash and zero submit overnight  
- **risk:** Fake COMPLETE to exercise `hydra-refill` → empty evidence, policy breach, pollutes queue  
- **risk:** Auth0 personal/product tenant (`manage.auth0.com`) confused with CIC BB → OOS traffic  
- **risk:** Whip/pruner “alive” mistaken for progress theater while all gates closed  
- **risk:** Overnight L3 j64 spam on third-party / unauth spider without new scope text → cost + noise, not gold  

## Freeze snapshot (data-walk)

| Lane | Program | State | Ship | Gate | Agent overnight if closed |
|------|---------|-------|------|------|---------------------------|
| stack H1 | Aiven MBB | MAPPED | `143b7d8` | free dual ninja + op titles A/B API (**missing**) | NEXT-TICK checklist fidelity only; no fake project probes |
| wrap H2 | Auth0 CIC | MAPPED | `301911b` | CRED-STATE **REQUESTED** (not ASSIGNED); Gmail empty | CRED pulse titles-only; unauth map already done — stop spider |
| grok H3 | Google VRP Drive | MAPPED | `2ad5517` | dual research Google (one usable login path) | FIRST-5 only if A/B; else negative-close pack / redacted template |

**Human doors (HUMAN-GATES):** all three still closed.  
**tmux:** `hydra` 8 windows, `sekhmet` 2, `janitor` 1 (2026-08-09 create stamp).  
**/tmp:** ~4% use — headroom fine.  
**Distill 2026-08-08:** thin (`oauth-callback-audit`, `overnight-gold-farm` scaffold) — no COMPLETE path.  
**Catalog:** open-bug-bounties `data/` platform~768 + independent~60 + web3~273; **do not page-walk 1100**.

---

# Strange connections (non-obvious)

1. **Refill never stress-tested** — zero COMPLETE means the 1→2 machine is unproven live. Overnight gold_ev does **not** require firing refill; forcing it is anti-gold.  
2. **Aiven is day-0 cash EV, Google is draft proximity** — ROI rank #1 Aiven (avg rewarded ~$4.1k, free tier authz class) vs HYDRA “closest draft” Google. Connector overnight: **OVERFIT primary = Aiven for gold_ev_per_hour**; secondary passive = Google **only if** second session appears; Auth0 = async email lottery (lowest gate_factor until ASSIGNED).  
3. **Dual-auth freeze list is the hydra backlog** — freezes 2/3/6 = Aiven/Auth0/Google. Idle work that re-documents freezes is thrash; idle work that **pre-builds Proton/MSRC/Atlassian map packs for refill** is fertile.  
4. **Whip op probes are a free human-gate sensor** — every tick already logs `op_aivenA/B`; when titles flip no→yes, that is the overnight unblock signal. Do not poll BC Get Credentials UI.  
5. **oauth-callback-audit is a hygiene negative** — paste tokens = process failure, not product CRIT. Teaches ship_hygiene without hunting 1100 programs.  
6. **Three lanes × fertile TOUCH + REPORT&lt;5m policy** already implements “no thrash”; idle should **not** invent new whip branches — only EV-QUEUE + overfit execute + sanitize/ship.  
7. **Atlassian free `bugbounty-test-*` is the cheapest *unstarted* human door** in keep-8 after the three locks — higher overnight prep EV than re-spidering Auth0 CIC unauth.  
8. **Catalog JSON has zero reward fields** — any overnight “refresh data” improves ship_hygiene/substrate, **not** EV rank; never confuse npm refresh with gold.

---

# EV rank recommendation (OVERFIT)

**Rubric (M02):**  
`score = payout_proxy(0–2) × skill_fit(0–2) × gate_factor × surface(0–2) / max(effort,1)`  
`gate_factor`: 1.0 open · 0.4 human_one_click · 0.15 async_email · 0.05 hard_captcha / second_acct_missing  

| Rank | Program | payout_proxy | skill_fit | gate_factor (now) | surface | effort | **score (approx)** | Overnight agent move |
|------|---------|--------------|-----------|-------------------|---------|--------|--------------------|----------------------|
| **OVERFIT** | **Aiven** | 1.6 (avg cash table) | 2.0 authz | **0.05** (dual acct missing) | 1.5 | 1 | **~0.24** live; **~2.4 if dual opens** | Primary: stay ready on NEXT-TICK + FIRST-5 + DRAFT; pulse op titles only |
| 2 | Google Drive VRP | 1.4 ceiling/web | 2.0 | **0.05–0.4** (one of two accts) | 1.8 | 1.2 | ~0.2–1.7 | Secondary: if A/B ready → T1–T5; else negative-close pack |
| 3 | Auth0 CIC | 1.3 identity | 2.0 | **0.15** async | 1.6 | 1 | ~0.6 theoretical post-assign | CRED-STATE poll only; no login |
| 4 | Proton (refill) | 1.5 | 1.5 | 0.4 own accts | 1.2 | 1.5 | map-only prep | FULL scope → playbook under distill (idle) |
| 5 | Atlassian | 1.2 | 1.8 | 0.4 site create | 1.5 | 1.5 | map-only prep | `bugbounty-test-*` checklist (idle) |
| 6–8 | MSRC / Shopify / GitLab⊕Dropbox | keep-8 tail | — | CAPTCHA/tenant/export | — | — | no overnight thrash | scope fidelity only |

**Lock line for EV-QUEUE / HYDRA:**

```
OVERFIT=Aiven lane=stack H1
SECONDARY_PASSIVE=Google-VRP-Drive lane=grok H3
TERTIARY_ASYNC=Auth0-CIC lane=wrap H2
REFILL_PREP=[Proton, Atlassian, MSRC] map-only
KILL=[YWH passport, pure SC+KYC, Sherlock race, alpha-1100 crawl]
```

**Rationale vs prior HYDRA “Google closest draft”:** draft proximity ≠ gold_ev. Google needs dual sessions same as Aiven needs dual ninja; Aiven has **proven avg reward + free-tier authz class + complete FIRST-5/OWN-ASSET/DRAFT**. When either dual gate opens, Aiven still wins expected $/agent-hour. If **only** Google A/B appears overnight, pivot OVERFIT execution to H3 without re-ranking membership.

---

# Max 3 cheap actions (agent-owned, no thrash)

1. **Write + freeze `~/.xbgst/hydra-bounty/tracker/EV-QUEUE.md`** from the table above (keep-8 only, gate flags, single OVERFIT line). Unblocks M02/M03; pure file work; harm none.  
2. **Idle fertile pack (NOT recon thrash):** under `bounty-distill/2026-08-08/overnight-gold-farm/`, author map-only playbooks for **Proton + Atlassian** (scope extract FULL/PARTIAL, own-asset checklist templates, dual-auth reuse pointers) + run secret-gate + `tmp-sanitize` on the folder. Optional: `npm run refresh` catalog only if network safe — treat as hygiene, not rank.  
3. **Non-thrash gate sensor only:** ensure whip/pruner loops stay green; update CRED-STATE / HUMAN-GATES timestamps from **title presence** (op list titles / Gmail subject scan for assign — **no** reveal, **no** Get Credentials click loop). On first `op_aivenA=yes` + B, hand M05 execute to lane executor — do not pre-probe without tokens.

**Explicit non-actions:** no CAPTCHA, no BC credential modal spam, no unauth re-spider Auth0/Aiven/Google, no fake COMPLETE, no 1100-program page walk, no personal Auth0 tenant.

---

# Idle-utilization work that is NOT recon thrash

| Work | Axis | Why not thrash |
|------|------|----------------|
| EV-QUEUE + HYDRA OVERFIT lock | gold_ev, time_to_first_submit | Rank once; freezes alpha walk |
| Proton / Atlassian / MSRC **map-only** playbooks for refill | substrate_fertile, idle_utilization | Prep next 1→2 without touching closed H1–H3 doors |
| Dual-auth playbook **pointer index** (paths only) into overnight folder | evidence_fidelity | Reuse DONE race; no re-race |
| Secret-gate + sanitize + milestone-ship **dry-run** of overnight folder | ship_hygiene, safety | Ship path green without inventing findings |
| sekhmet-luna `tasks.md` on **already-owned scope text** (FIRST-5 expand, DRAFT skeleton fill, negative-close templates) | substrate_fertile | j64 on local text; no live third-party exploit |
| Dawn rollup template (CONNECTOR + HYDRA status + BLOCKED reasons) | human_gate_throughput | One page for human morning doors |
| Pruner/whip keep-alive + /tmp GC | substrate_fertile | Observed dead gap 00:40Z — fertility is real ops |

**Not idle-gold:** re-fetch platform_programs.json for “ideas”; re-map UNAUTH-DOORS; L3 expand without new human scope; multi-product Google VRP fan-out; Okta SPA Set5 overnight.

---

# Dissent vs thrash

| Role / model likely says | Connector reply |
|--------------------------|-----------------|
| “Google is closest to draft — OVERFIT Google” | Draft proximity without second account is same gate class as Aiven dual. Prefer Aiven for **gold_ev**; execute Google **if** A/B opens first. |
| “Need more unauth / L3 map depth on H1–H3” | Maps shipped (`143b7d8`/`301911b`/`2ad5517`). More spider = thrash. Only door-open moves COMPLETE. |
| “Walk catalog / refresh 1100 to find gold” | Inventory has **no reward fields**; keep-8 is SSoT. Refresh = hygiene only. |
| “Mark COMPLETE so refill exercises” | Refuse. MAP≠COMPLETE. Empty COMPLETE destroys evidence_fidelity and safety. |
| “Auth0 is identity-day #2 ROI — push harder” | CRED REQUESTED ~1d+; async 0.15 gate. Poll inbox/titles; do not thrash modal. After 24h still empty → wrap passive, refill-prep Proton/Atlassian. |
| “Start Shopify/GitLab deep overnight” | CAPTCHA / H1 export / XOR Dropbox — hard gate_factor; kills idle quality. |
| “Whip should spawn harder when idle” | Whip policy already correct (`no_spawn` when fertile + gates closed). Spawning is thrash. |
| “Ceiling max = rank” | payout_proxy uses **Aiven avg rewarded + realistic web classes**, not marketing max alone. |

---

# Rationale (strange angle)

The farm already **solved recon**. Dual-auth DONE, three lanes MAPPED, whip non-thrash, pruner fertile, ships on main, keep-8 frozen. The dumb user order (“walk pages + sleep farm”) fails because money is behind **three human keys** that agents cannot mint. Smart overnight gold is therefore **orthogonal to hunting**: (1) freeze EV so nobody alpha-walks 1100, (2) park execution on Aiven as OVERFIT with op-title as the only sensor, (3) spend CPU on **refill-prep + sanitize + ship hygiene** so the first human door-open converts in minutes, not another map day. Gold_ev_per_hour rises by **eliminating thrash options**, not by adding programs.

**Success boundary overnight (honest):** EV-QUEUE frozen · OVERFIT=Aiven locked · whip/pruner green · zero fake COMPLETE · zero secret leakage · optional APPROVED ship of plan+EV+status distill · if a door opens, FIRST-5 evidence path started — else BLOCKED(human) with recovery, not theater.

---

# Evidence pointers (no secrets)

- Plan: `~/.xbgst/plans/2026-08-08-overnight-gold-farm.md`  
- Prior connector: `~/.xbgst/hydra-bounty/CONNECTOR.md`  
- Gates: `HUMAN-GATES.md` · `lanes/wrap/auth0/CRED-STATE.md` · `lanes/stack/aiven/OWN-ASSET.md` · `NEXT-TICK.md`  
- Tracker: `tracker/HYDRA.md` · **EV-QUEUE.md MISSING (M02)**  
- ROI keep-8: `bounty-distill/2026-08-07/ROI.md` · `HUNT-NOW.md`  
- Dual-auth: `bounty-distill/2026-08-07/dual-auth-runners/AUTONOMOUS-READY.md`  
- Whip: `logs/WHIP.log` (2026-08-09T01:01Z green; op A/B no)  
- Lane STATUS: `lanes/{stack,wrap,grok}/**/STATUS.md` — all MAPPED/PARTIAL  

---

# Next (for judge / planner / executors)

| Who | Move |
|-----|------|
| scribe/planner | Land `tracker/EV-QUEUE.md` with OVERFIT=Aiven |
| executor-ops | Keep whip+pruner; no spawn thrash |
| distiller | overnight-gold-farm folder: playbooks + secret gate |
| connector (later) | M06 title-only pulse; M09 dawn rollup |
| human | Any of: Aiven dual free, Auth0 ASSIGNED, Google A/B — single door unblocks M05 |

**planner-gate residual:** advisory · risks R1–R9 still open · no COMPLETE · OVERFIT recommendation Aiven pending EV-QUEUE file write.
