GODSPEED inject required.
Produce a compact authz/IDOR test matrix markdown for Aiven API (api.aiven.io) between two OWN accounts.
Columns: endpoint class, method, expected deny, severity if 200. Rows: project get, service list, users, invite, kafka acl, permissions.
Own free project only. No third-party hosts. No DoS. Max 50 lines. Checklist format.
