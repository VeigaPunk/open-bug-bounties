# HUMAN GATES — what unblocks money (hydra)

Maps are done. **COMPLETE / submit** needs these human doors:

1. **Aiven (stack)** — research Bugcrowd ninja account: free-tier project + Postgres; second member account for authz matrix. Creds only in 1Password titles `Aiven BB Account A API` / `B API` (create if missing). Follow `lanes/stack/aiven/NEXT-TICK.md`.
2. **Auth0 CIC (wrap)** — Bugcrowd **Get Credentials** for Auth0 engagement: still **REQUESTED** (Gmail 2026-08-07T23:12Z: no assign mail). When email/BC shows assigned, store in op:// and update `lanes/wrap/auth0/CRED-STATE.md` → then FIRST-5 on CIC tenants only.
3. **Google VRP (grok)** — two research Google accounts; run Drive FIRST-5 from `lanes/grok/google-vrp/FIRST-5-TESTS.md` (browser local / burner). op has one Google with password; second is bughunters URL only.

## Agent-cleared (not money, but no longer stuck)

| Item | Status |
|------|--------|
| tmux `hydra` duplicate clients | deduped to 1 |
| sekhmet fixed-id `namespace already exists` | GC + tasks-v3 UUID |
| M08 `hydra-whip.sh` missing | installed + 119s loop |
| Passive door pulse | re-run 401/200 matrix |
| BC identity verification | email confirms success (eligible for ID-gated programs) |

Until human doors open, hydra stays **MAPPED**, ships audit trail, L3 fertilizes, whip keeps substrate warm. **No fake COMPLETE.**
